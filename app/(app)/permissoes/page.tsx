const permissions = ['Visualizar dashboard', 'Gerenciar municípios', 'Gerenciar unidades', 'Gerenciar convênios', 'Resolver pendências', 'Enviar documentos', 'Administrar usuários'];
const roles = ['Administrador', 'Gestor', 'Analista', 'Leitura'];
export default function PermissionsPage() {
  return <section className="management-page"><header className="management-header"><div><p className="eyebrow">ADMINISTRAÇÃO</p><h1>Papéis e permissões</h1><p>Defina o que cada papel pode visualizar e alterar.</p></div><button className="primary-action" type="button">Salvar alterações</button></header><div className="permission-card"><aside><h2>Papéis</h2>{roles.map((role, index) => <button className={index === 0 ? 'selected' : ''} key={role} type="button"><span>{role}</span><small>{index === 0 ? 'Acesso completo' : 'Permissões configuráveis'}</small></button>)}</aside><div className="permission-matrix"><header><div><h2>Administrador</h2><p>Acesso completo ao ambiente Voyager.</p></div><span className="status status-ativo">Ativo</span></header><table><thead><tr><th>Permissão</th><th>Visualizar</th><th>Criar</th><th>Editar</th><th>Excluir</th></tr></thead><tbody>{permissions.map((permission) => <tr key={permission}><td>{permission}</td>{['view','create','edit','delete'].map((action) => <td key={action}><input aria-label={`${permission}: ${action}`} defaultChecked type="checkbox" /></td>)}</tr>)}</tbody></table></div></div></section>;
}

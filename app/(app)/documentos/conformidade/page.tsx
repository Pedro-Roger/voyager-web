const requirements = [['Plano de trabalho aprovado', 'Conforme'], ['Certidão de regularidade fiscal', 'Incompleto'], ['Relatório de execução financeira', 'Pendente'], ['Declaração do responsável técnico', 'Conforme']];
export default function CompliancePage() {
  return <section className="management-page compliance-page"><header className="management-header"><div><p className="eyebrow">DOCUMENTOS</p><h1>Checklist de conformidade</h1><p>Alpha Central Expansion · AG-2024-81</p></div><button className="primary-action" type="button">Enviar para análise</button></header><div className="compliance-layout"><main className="detail-card"><header><div><h2>Requisitos obrigatórios</h2><p>2 de 4 itens conformes</p></div><span className="progress-value">50%</span></header><div className="progress-track"><span /></div><ul className="requirement-list">{requirements.map(([name,status]) => <li key={name}><span className={`requirement-icon ${status === 'Conforme' ? 'ok' : ''}`}>{status === 'Conforme' ? '✓' : '!'}</span><div><strong>{name}</strong><small>{status === 'Conforme' ? 'Documento validado' : 'Ação necessária'}</small></div><span className={`status status-${status.toLowerCase()}`}>{status}</span></li>)}</ul></main><aside className="detail-card"><h2>Anexar documento</h2><label>Tipo documental<select defaultValue=""><option disabled value="">Selecione o requisito</option>{requirements.map(([name]) => <option key={name}>{name}</option>)}</select></label><label className="mini-drop">Solte o arquivo aqui<input hidden type="file" /></label><button className="primary-action" type="button">Adicionar documento</button></aside></div></section>;
}

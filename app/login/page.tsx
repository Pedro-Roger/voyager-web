import { LoginForm } from '../../components/login-form';

export default function LoginPage() {
  return (
    <main>
      <h1>Entrar no Voyager</h1>
      <p>Acesse painel operacional do observador.</p>
      <LoginForm />
    </main>
  );
}
